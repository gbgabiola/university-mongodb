# $graphLookup: maxDepth and depthField

## Problem:
Which of the following statements are incorrect? _check all that apply_

## Check all answers that apply:
1. **depthField** determines a field, which contains the value number of documents matched by the recursive lookup
2. **depthField** determines a field, in the result document, which specifies the number of recursive lookup needed to reach that document
3. **maxDepth** allows to specify the number of recursive lookups
4. **maxDepth** only takes **$long** values


## Answer
1. **depthField** determines a field, which contains the value number of documents matched by the recursive lookup
4. **maxDepth** only takes **$long** values

## Detailed Answer:
